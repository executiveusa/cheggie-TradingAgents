# ChatGPT Trade Monorepo Additions
- `apps/web`: Next.js landing page with EN/SR toggle and demo call.
- `services/api`: FastAPI wrapper exposing `/api/health` and `/api/analyze`.
- `services/compute-rust`: Rust RSI helper module scaffold.
- `apps/mobile`: Expo mobile scaffold using the same backend endpoint.
